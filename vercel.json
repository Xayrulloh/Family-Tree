{
  "version": 2,
  "buildCommand": "npx nx build web",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist/apps/web",
  "framework": "vite",
  "ignoreCommand": "echo \"$VERCEL_GIT_COMMIT_REF\" | grep -q '^dependabot/' && exit 1 || exit 0"
}
