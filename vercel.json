{
  "version": 2,
  "buildCommand": "npx nx build web",
  "installCommand": "pnpm install --frozen-lockfile",
  "outputDirectory": "dist/apps/web",
  "framework": "vite",
  "ignoreCommand": "npx nx print-affected --base HEAD~1 --target=build | grep 'web' || echo 'no-build'"
}
